const code = {
    javascript: {
        code: `import Badge from "@rad-ui/ui/Badge"

const BadgeExample = () => (
    <div>
        <Badge>Badge</Badge>
    </div>
)`
    },
    css: {
        code: `todo`
    },
}

export default code;